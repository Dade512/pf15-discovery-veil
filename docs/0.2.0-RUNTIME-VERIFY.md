# 0.2.0 Runtime Verification — pf15-discovery-veil (Perception Gate MVP)

Date: 2026-06-15 · Verifier: Claude (Opus 4.8), driving Michael's live Vivaldi (GM) + Chrome
(player) seats remotely (Michael at work, authorized in chat). Foundry 13.350 / pf1 11.11.
World restored to its pre-test state afterward.

Setup: module junctioned into Foundry's modules dir + enabled by Michael; `perceptionEnabled`
set true via GM console. Test creature: the goblin (combatant `knTHKTrICYBxgozA`).

## Static gates (pre-live)
- Phase 1 state-layer harness: 22/22 (secret rejection, GM/active-GM gating, schema refusal,
  world partitioning, fail-closed).
- Phase 2 `node --check`: all six scripts clean; `en.json` valid.
- Rendering gating harness: 7/7 (incl. un-spotted non-GM NOT overridden — no over-broad show).

## Live results (two-client)
| Check | Result |
|-------|--------|
| HUD controls render (GM) | no gate -> "Mark undetected"; gated -> Manage / Reveal / Clear. PASS |
| Mark Undetected | gate = {state: undetected, priorHidden: false, hiddenByModule: true, spottedBy: {}}; native `hidden` = true. PASS |
| Un-spotted player cannot see | offline gating 7/7 + 0.1.1 spike (native hidden -> player `visible` = false). PASS (offline + spike) |
| Spotted player sees (override) | DXWarlock (spotted): goblin `visible` = YES, `mesh.visible` = YES, and the override SURVIVES a `refreshVisibility`. PASS (live) |
| GM sees natively | GM goblin `visible` = YES. PASS |
| Reveal Globally | state -> globallyRevealed; native `hidden` restored to false (visible to all). PASS |
| Clear gate | gate removed (null). PASS |
| F-6: pre-concealed token not unhidden | a token the GM pre-hid -> `hiddenByModule` = false; Clear leaves it concealed (`PASS_token_remained_concealed_on_clear`). PASS (live) |
| Privacy | player reads registry = only {state, spottedBy, priorHidden, hiddenByModule}; NO forbidden keys; private store = 0 worlds on the player. PASS |
| Restore | goblin `hidden` = false; registry perception keys = 0. Done |

## Verdict
**0.2.0 Perception Gate MVP — RUNTIME-VERIFIED two-client.** Native `hidden` base + per-client
show-override (reapplied on `refreshVisibility`) works; F-6 ownership preserved; no secrets on
replicated surfaces. Recommend version bump to 0.2.0 + commit/tag (the module is not yet a git
repo — needs init + a new remote).

## Notes
- The "Manage Spotted" dialog was exercised via its state path (`markSpotted`) and the button's
  render was confirmed; the full checkbox->save click was not separately driven (same state
  path). Mark-spotted in 0.2.0 is manual; Perception-roll capture is 0.3.0.
- Override reapplies on `refreshVisibility` (the hook-reapply pattern, as designed).
- One leftover test chat message (an earlier 0.1.1 Perception roll) may remain in the log — safe
  to delete.
